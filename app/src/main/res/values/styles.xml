<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="SampleTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/sky_blue_color</item>
        <item name="colorPrimaryDark">@color/sky_blue_color</item>
        <item name="android:subtitleTextStyle">@style/ActionBarSubtitle</item>
    </style>

    <style name="ActionBarSubtitle" parent="@android:style/TextAppearance.Holo.Medium">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="splash_style" parent="@android:style/Theme.NoTitleBar.Fullscreen"></style>
</resources>
